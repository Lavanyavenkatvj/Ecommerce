# netlify.toml — Netlify deployment configuration
# This tells Netlify to serve index.html for all routes,
# so the client-side router (hash-based) works correctly.

[build]
  publish = "."

[[redirects]]
  from = "/*"
  to   = "/index.html"
  status = 200
